import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://sisodiaa.github.io",
  base: "/frontend-mentor-huddle-landing-page-with-alternating-feature-blocks",
  experimental: {
    assets: true,
  },
});
